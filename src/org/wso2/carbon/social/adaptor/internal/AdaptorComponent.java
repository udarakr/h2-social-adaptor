/*package org.wso2.carbon.social.adaptor.internal;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.osgi.framework.BundleContext;
import org.osgi.service.component.ComponentContext;

/**
 * Registering {@link SocialActivityService}
 * 
 * @scr.component name="org.wso2.carbon.social.component" immediate="true"
 **/
/*public class AdaptorComponent {

	private static Log log = LogFactory.getLog(AdaptorComponent.class);

	protected void activate(ComponentContext context) {
		BundleContext bundleContext = context.getBundleContext();
		bundleContext.registerService(AdaptorComponent.class,
				new AdaptorComponent(), null);
		if (log.isDebugEnabled()) {
			log.debug("Social Activity service is activated  with Adaptor implementation");
		}

	}
}
*/
